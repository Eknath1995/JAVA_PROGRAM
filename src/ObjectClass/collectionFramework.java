package ObjectClass;

public class collectionFramework {
	//collection:-
	// collection frame work is ready made utility written in java classes and interfaces.
	
	// *Methods in collection interface.
	
		// 1] boolean    add(object o);
		// 2] boolean    addAll(collection c);
		// 3] int        size();
		// 4] boolean    isempty();				// all methods are public abstract.
		// 5] void       clear();
		// 6] boolean    contains(object o);
		// 7] boolean    remove(object o);
		// 8] boolean    remove(collection c);
	
	
	//stmt:- 
	  		// * collection in java refers to group of objects.
	
			// Q- why do we choose collection over an array??
		   //   A- 1)Bcoz array is fixed in size.
		   //      2)we usually use array for primitive data.

}